# Reference: OpenCode

Repository-local reference pointer for OpenCode configuration, runtime behavior, and harness assumptions.

Prefer `README.md`, `opencode.json`, and canonical docs in `.opencode/docs/` before relying on external summaries. Treat `docs/` as reference-only mirror material.
