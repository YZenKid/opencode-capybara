# Reference: MCP

Repository-local reference pointer for configured MCP surfaces and safety posture.

Prefer `.opencode/docs/MCP.md` and `opencode.json` as the canonical sources. Treat `docs/` as reference-only mirror material.
