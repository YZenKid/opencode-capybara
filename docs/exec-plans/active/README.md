# Active Execution Plans

Use `.opencode/plans/` as the current durable task-plan location.

This folder documents the target-state information architecture for active execution plans if the repository later chooses to mirror or surface plan states inside `docs/`.
