# Completed Execution Plans

Use `.opencode/plans/` and `.opencode/evidence/` as the current durable implementation artifacts.

This folder documents the target-state information architecture for completed execution plans if the repository later chooses to mirror or surface plan states inside `docs/`.
